# Mini Recruitment Dashboard (Frontend)

- React + Vite + TypeScript
- TailwindCSS

## Env
Create `.env` (or set build env on Vercel):

```bash
VITE_API_URL=http://localhost:8080
```

## Run locally
```bash
npm install
npm run dev
```

